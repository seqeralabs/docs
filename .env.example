# Build Performance Controls
# Set these in .env.local for faster local development builds

# Increase for faster local builds (if you have enough RAM)
# DOCUSAURUS_SSG_WORKER_THREAD_COUNT=4
# RSPACK_PARALLELISM=4
# DOCUSAURUS_MINIFY_WORKERS=4

# Memory limit (increase for local development)
# NODE_OPTIONS=--max-old-space-size=4096

# Feature flags to speed up local builds
# EXCLUDE_CHANGELOG=true
# EXCLUDE_PLATFORM_API=true
# EXCLUDE_MULTIQC=true
# LIMIT_VERSIONS=true
