- 
  Environment variable:            '`TOWER_CONTENT_URL`'
  Description: > 
    URL to securely download user-generated content (must be a subdomain of `TOWER_SERVER_URL`).
  Value: 
- 
  Environment variable:            '`TOWER_DEFAULT_WORKSPACE_ID`'
  Description: > 
    ID of the default workspace where users will land after logging in. Requires users to be members of the given workspace.
  Value:                '<your-workspace-id>'
- 
  AWS Parameter Store:            '`{prefix}/tower/admin/default-workspace-id`'
  Description: > 
    ID of the default workspace where users will land after logging in. Requires users to be members of the given workspace.
  Value:                '<your-workspace-id>'
- 
  AWS Parameter Store:            '`{prefix}/tower/contentUrl`'
  Description: > 
    URL to securely download user-generated content (must be a subdomain of `/tower/serverUrl`).
  Value:  
- 
  tower.yml:            '`tower.admin.default-workspace-id`'
  Description: > 
    ID of the default workspace where users will land after logging in. Requires users to be members of the given workspace.
  Value:                '<your-workspace-id>'
- 
  AWS Parameter Store:            '`tower.contentUrl`'
  Description: > 
    URL to securely download user-generated content (must be a subdomain of `tower.serverUrl`).
  Value: 
